.DS_Store
*.gguf
*.safetensors
.env
.env.local
*.log

# Local recording artifacts — only the canonical demo gif is committed
demo/smoke.gif


# Added by cargo

/target

# uv project
.venv/
__pycache__/
*.pyc

# pi extensions — commit settings.json (the manifest) but not the resolved node_modules
.pi/npm/node_modules/
.pi/npm/package-lock.json
.pi/npm/
