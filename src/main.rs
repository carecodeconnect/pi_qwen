// Placeholder crate. Nothing in the sandbox runs through this binary — inference is
// llama.cpp / ollama via the scripts (see README); the Rust engine route is the
// external `mistralrs-server` binary documented in docs/07-engines.md.
fn main() {
    println!("Hello, world!");
}
